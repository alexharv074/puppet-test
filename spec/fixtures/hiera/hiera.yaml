---
version: 5
defaults:
  datadir: data
  data_hash: yaml_data
hierarchy:
  - name: "YAML data: environments, stages, roles, common"
    paths:
      - "roles/%{role}/common.yaml"
      - "roles/common.yaml"
      - "common.yaml"
